// Shared types re-exported from GameCanvas for backward compatibility.
// The canonical definitions now live in game/GameConfig.ts.

export type { BuildingEntry } from '@/game/GameConfig'
export type SpecialAction = 'noticeboard' | 'rest'
